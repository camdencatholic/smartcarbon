import { Questrial, Inter_Tight, Cinzel_Decorative } from 'next/font/google'
export const FontSerif = Questrial({ subsets: ['latin'], weight: "400" })
export const FontDisplay = Inter_Tight({ subsets: ['latin'], weight: "variable" })
export const FontOrnate = Cinzel_Decorative({ subsets: ['latin'], weight: "400" })